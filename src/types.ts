export type Collection = {
    createdAt: Date 
    name: string
    summary: string
    _id: string
}

export type Category = {
    createdAt: Date 
    name: string
    summary: string
    _id: string
}